import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'games-community-and-store',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
